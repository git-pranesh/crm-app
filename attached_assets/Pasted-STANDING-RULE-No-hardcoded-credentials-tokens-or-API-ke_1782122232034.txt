STANDING RULE: No hardcoded credentials, tokens, or API keys in any 
file. grep -rn "eyJ" client/src/ must return zero matches — run this 
before submitting results.

Reports UI redesign. All 14 report endpoints verified built in Module 
10. Confirm these return real data before writing any code:

  GET /api/reports/lead_summary
  GET /api/reports/pipeline
  GET /api/reports/conversion
  GET /api/reports/timeline_performance
  GET /api/reports/designer_performance
  GET /api/reports/bl_performance
  GET /api/reports/source_performance
  GET /api/reports/campaign_performance
  GET /api/reports/inactive_leads
  GET /api/reports/meeting_performance
  GET /api/reports/sales_cycle
  GET /api/reports/lead_aging
  GET /api/reports/monthly_trend
  GET /api/reports/offer_performance
  GET /api/reports/lead_summary/export (CSV)

All accept query params: startDate, endDate, designerId, blId, 
source, stage, projectType, location.

If any missing: STOP and report — do not stub.

Check package.json for charting libraries already installed.
Expected from prior modules: recharts (confirmed in Module 0 stack).
Use recharts throughout. Do NOT install new charting libraries.

---

PAGE STRUCTURE (/reports)

Single scrollable page — NOT a tab-per-report pattern. All report 
sections visible on scroll, matching the mockup video exactly.

### Global filter bar (sticky below top nav):
  Date range picker — presets: This Month | This Quarter | 
    Last Financial Year | Custom Range
  On change: re-fetch ALL report sections with new date params.
  Default: This Month.
  Right side: "Export CSV" per section (individual export buttons, 
    not one global export).

---

SECTION 1 — SALES PIPELINE (matches mockup screenshot exactly)

Section header: "Sales pipeline" + subtitle "Conversion, performance 
& trends across the pipeline."

Row 1 — 4 KPI cards (from /api/reports/lead_summary + /conversion):
  Total Leads — count
  Onboarded — count of leads that reached ONBOARDING stage
  Conversion — leads→onboarded % (e.g. "14%", subtitle "Leads → onboarded")
  Active Designers — count of users with role DESIGNER and isActive=true

Row 2 — two charts side by side:
  LEFT: "Lead source performance" card
    Bar chart (vertical): x-axis = source names, y-axis = lead count
    Each bar grouped: leads count (bar height) 
    Table below chart: SOURCE | LEADS | SHARE % columns
    Source rows: Meta Ads, Google Ads, Referral, Walk-in, Website, 
      Manual (whatever exists in DB)
    "Export CSV" button top right of card
    Data: GET /api/reports/source_performance

  RIGHT: "Stage distribution" card
    Bar chart (vertical): x-axis = stage names (New/Effective/MQL/
      DQL/Prop.Ready/Prop.Presented/Onboarding), y-axis = lead count
    Each bar a distinct color (reuse Pipeline kanban stage colors)
    Table below chart: STAGE | LEADS | SHARE % columns
    "Export CSV" button top right of card
    Data: GET /api/reports/pipeline (use stage counts)

---

SECTION 2 — DESIGNER PERFORMANCE

Section header: "Designer performance" + subtitle "Assigned leads, 
design meetings (DQL / PP) and conversion per designer."
"Export CSV" top right of section card.

Table columns (exact from mockup):
  DESIGNER (avatar + name) | ASSIGNED | DQLS | PPS | ONBOARDED | 
  CONV. %

Each row = one designer. Sort by ASSIGNED desc by default.
Conv% = onboarded / assigned, formatted as "21.7%".
No pagination needed if team is small (<20 designers).

Data: GET /api/reports/designer_performance
Map response fields to columns — if field names differ from above, 
map them correctly, do not rename backend fields.

---

SECTION 3 — MONTHLY TREND

Section header: "Monthly trend" + subtitle "New leads created each 
month."
"Export CSV" top right.

Two-panel layout:
  LEFT: Line chart
    x-axis = month labels (Feb 2026, Mar 2026, Apr 2026, etc.)
    y-axis = lead count
    Single line, dots at each data point
    recharts LineChart + CartesianGrid + Tooltip + XAxis + YAxis

  RIGHT: Table
    MONTH | LEADS CREATED columns
    One row per month, sorted chronologically
    Current month shows partial count (not projected)

Data: GET /api/reports/monthly_trend
If quarterly/half-yearly aggregation exists in response, show it 
as a toggle above the chart: Monthly | Quarterly | Annual — 
switching recalculates x-axis grouping client-side from the 
monthly raw data. No extra API call needed for this.

---

SECTION 4 — OFFER PERFORMANCE

Section header: "Offer performance" + subtitle "Leads tagged to each 
promotional offer and how many onboarded."
"Export CSV" top right.

Horizontal bar chart:
  y-axis = offer names (Pongal Special, Diwali Dhamaka, EID Offer, 
    Women's Day Offer, etc. — whatever exists in DB)
  x-axis = count
  Each bar = terracotta/orange fill (brand color)
  Bar width proportional to count
  Count label at end of each bar

Data: GET /api/reports/offer_performance
Empty state: "No offers have been applied to leads yet." if no data.

---

SECTION 5 — CAMPAIGN PERFORMANCE (UTM)

Section header: "Campaign performance" + subtitle "Which Meta and 
Google campaigns are generating the best leads."
"Export CSV" top right.

Table:
  CAMPAIGN | SOURCE | LEADS | ONBOARDED | CONV. %

Data: GET /api/reports/campaign_performance
Empty state: "No UTM campaign data available yet." if no records — 
many leads may not have UTM data from test seeding, that's fine.

---

SECTION 6 — MEETING PERFORMANCE

Section header: "Meeting performance" + subtitle "Meetings scheduled 
vs completed vs no-show per designer."
"Export CSV" top right.

Table:
  DESIGNER | SCHEDULED | COMPLETED | NO-SHOW | COMPLETION RATE %

Completion rate = completed / scheduled %.
Data: GET /api/reports/meeting_performance

---

SECTION 7 — INACTIVE LEADS

Section header: "Inactive leads" + subtitle "Leads that dropped off, 
at which stage, and why."
"Export CSV" top right.

Table:
  LEAD ID | NAME | STAGE AT INACTIVATION | REASON | FEEDBACK 
  RECEIVED | FEEDBACK RESPONSE

FEEDBACK RECEIVED = Yes/No pill (green/grey)
FEEDBACK RESPONSE = text if responded, "—" if not

Aggregate strip above table:
  "N leads inactivated · N% feedback response rate"

Data: GET /api/reports/inactive_leads (already returns 
feedbackFormSentAt, clientResponded, feedbackResponse per G4 of 
Module 9 verification)

---

SECTION 8 — REMAINING REPORTS (collapsed by default)

The following 5 reports exist in the backend but are lower-priority 
for the UI. Render each as a simple collapsible card with:
  - Section header + "Export CSV" button
  - Collapsed by default — click to expand
  - When expanded: plain table from API response, no chart needed
  - No empty-state complexity — just "No data available" if 
    response is empty array

Reports in this group:
  BL Performance (GET /api/reports/bl_performance)
    Columns: BL NAME | LEADS MANAGED | CLOSED | AVG VALUE | 
    DISCOUNT REQUESTS | APPROVED | REJECTED
  
  Sales Cycle (GET /api/reports/sales_cycle)
    Columns: METRIC | DAYS
    Rows: Average lead-to-onboarding, Fastest, Slowest
  
  Lead Aging (GET /api/reports/lead_aging)
    Columns: LEAD ID | NAME | STAGE | DAYS IN STAGE | SLA THRESHOLD
    Only leads currently past their SLA threshold
  
  Timeline Performance (GET /api/reports/timeline_performance)
    Columns: METRIC | AVG DAYS
    Rows: Lead→DQL, DQL→PP, PP→Onboarding
  
  Conversion Funnel Detail (GET /api/reports/conversion)
    Columns: STAGE | LEADS | CONVERSION %
    One row per stage transition

---

ROLE SCOPING ON ALL SECTIONS:

BRANCH_HEAD/Admin: sees all data, no filter restrictions
BL: all sections scoped to their team (server handles this — 
  pass no extra params, backend role-scopes automatically)
DESIGNER: sees only designer_performance row for themselves, 
  meeting_performance for themselves — other sections hidden 
  (check which reports make sense for a designer and hide the 
  rest, don't show empty tables)
CRE: same as DESIGNER scoping — show only relevant sections

Implement role-based section visibility:
  const showFullReports = ['BRANCH_HEAD', 'BL'].includes(user.role)
  const showDesignerOnly = ['DESIGNER', 'CRE'].includes(user.role)
  Sections 1,5,6,7,8 (bl_performance) — showFullReports only
  Sections 2,3,6 (meeting_performance) — all roles (scoped by server)
  Section 4 (offers) — showFullReports only

---

AFTER IMPLEMENTATION:

1. Screenshot /reports as BRANCH_HEAD — full page scroll showing 
   all sections with real data. Paste multiple screenshots if 
   needed to cover the full page.

2. Screenshot Section 1 (Sales Pipeline) zoomed in — confirm 
   KPI cards show correct numbers and both charts render with 
   real bars/slices.

3. Screenshot Section 2 (Designer Performance) — confirm table 
   has rows with real designer names and numeric data.

4. Screenshot Section 3 (Monthly Trend) — confirm line chart 
   renders with correct month labels and a visible line. Toggle 
   to Quarterly view — screenshot that too.

5. Screenshot Section 4 (Offer Performance) — confirm horizontal 
   bars render. If no offer data in DB: seed one via 
   POST /api/offers + PATCH a lead's offer, then reload.

6. Click "Export CSV" on Section 2 (Designer Performance) — 
   confirm a CSV file downloads with correct headers and data.

7. Change date range from "This Month" to "Last Financial Year" 
   (or a wide custom range) — screenshot the KPI numbers 
   updating across all sections.

8. Screenshot /reports as DESIGNER — confirm only relevant 
   sections visible, no broken/empty sections shown.

9. Expand one collapsible Section 8 report (e.g. Sales Cycle) 
   — screenshot the expanded table.

10. List every file created/modified.

11. Confirm zero console errors on /reports for both BRANCH_HEAD 
    and DESIGNER login.

12. grep -rn "eyJ" client/src/ → paste output. Must be empty.