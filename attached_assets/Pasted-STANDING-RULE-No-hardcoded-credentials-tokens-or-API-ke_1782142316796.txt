STANDING RULE: No hardcoded credentials, tokens, or API keys in any 
file. grep -rn "eyJ" client/src/ must return zero matches before 
submitting results.

Fix exactly the 11 bugs listed below in order of severity.
Additive only — do not rewrite existing logic, extend it.
After all fixes: run prisma migrate dev if schema changed, 
prisma generate, restart server, confirm GET /api/health → 200.
List every file + line modified per bug fix.

---

BUG-001 — Lead Export CSV downloads wrong data
Files: server/src/routes/leads.ts, client/src/pages/LeadList.tsx

Server fix:
Add GET /api/leads/export — accepts same query params as GET /api/leads
(search, stage, source, designerId, blId, intent, projectType, 
location, dateRange). Fetches matching leads, converts to CSV, returns
with Content-Type: text/csv and Content-Disposition: attachment; 
filename="leads-export.csv".

CSV columns: leadId, name, phone, email, stage, status, source, 
estimatedValue, intentRating, assignedDesigner, assignedBL, 
createdAt, updatedAt

Client fix:
LeadList.tsx — change handleExportCSV to call 
/api/leads/export?${currentFilterParams} (same params already 
built for the list call). Trigger file download via blob URL.

Test: Apply stage=MQL filter, click Export CSV → downloaded file 
contains only MQL leads. Confirm Content-Type is text/csv.

---

BUG-002 + BUG-007 — WhatsApp templates always empty (shared root)
Files: server/src/routes/admin.ts, prisma/schema.prisma (if needed), 
server/src/routes/whatsapp.ts, client/src/pages/Inbox.tsx

Root cause A: GET /api/admin/whatsapp-templates returns empty array.
Root cause B: /api/leads/:id/whatsapp returns templates as string 
array not objects.

Fix A — seed default templates on startup if table/array is empty.
In server/src/routes/admin.ts, GET /api/admin/whatsapp-templates:
If the returned array is empty, return these 6 hardcoded defaults 
(these are fixed pre-approved templates, not user-editable):

[
  { id: "pre_call_intro", label: "Pre-call Introduction",
    body: "Hi {{name}}, this is {{agent}} from Interiors by DeX. Thanks for your interest in our interior design services. When is a good time to call?" },
  { id: "rnr_followup", label: "RNR Follow-up",
    body: "Hi {{name}}, tried reaching you regarding your interior design enquiry. Let me know a convenient time to connect." },
  { id: "meeting_confirmation", label: "Meeting Confirmation",
    body: "Hi {{name}}, your consultation with Interiors by DeX is confirmed. Our designer will meet you as scheduled. Looking forward to it!" },
  { id: "mom_sent", label: "MOM Summary",
    body: "Hi {{name}}, thank you for meeting with us today. Please find the minutes of our meeting shared via email." },
  { id: "onboarding_welcome", label: "Onboarding Welcome",
    body: "Welcome aboard, {{name}}! We're delighted to design your home. Your dedicated designer will keep you updated at every stage." },
  { id: "on_hold_notification", label: "On-Hold Notification",
    body: "Hi {{name}}, your project has been placed on hold until {{revivalDate}}. Reason: {{reason}}. We'll be in touch." }
]

This is safe — templates are read-only, no DB schema change needed. 
Return from in-memory constant if no DB table exists for templates.

Fix B — /api/leads/:id/whatsapp endpoint:
Find where it returns templates field. Change it from returning 
bare string IDs to returning objects { id, label, body } matching 
the same 6-template array above. Or simply remove the templates 
field from this endpoint — Inbox.tsx should fetch templates from 
/api/admin/whatsapp-templates separately (one call on mount, 
not per-lead). Whichever is cleaner, state your choice.

Fix C — Inbox.tsx:
On mount (not on every lead select), fetch 
GET /api/admin/whatsapp-templates and store in local state.
Template picker renders from this state.
Each template: show label as the picker option, populate body 
into textarea on select.

Test: Open /whatsapp, open template picker → 6 templates listed.
Select "Meeting Confirmation" → textarea pre-fills with body text.

---

BUG-003 — No-show reason silently discarded
Files: prisma/schema.prisma, server/src/routes/meetings.ts, 
client/src/pages/Meetings.tsx (and LeadDetail MeetingsTab)

Schema fix:
Add to Meeting model:
  noShowReason  String?

Run: npx prisma migrate dev --name add_no_show_reason_to_meeting

Server fix — meetings.ts PATCH /:id/status:
In the NO_SHOW branch (currently missing server-side validation):
  if (!body.noShowReason?.trim()) {
    return res.status(400).json({ 
      error: "No-show reason is required" 
    })
  }
  await prisma.meeting.update({
    data: { 
      status: 'NO_SHOW',
      noShowReason: body.noShowReason.trim()
    }
  })

Client fix — Meetings.tsx:
Change no-show submission to send { status: 'NO_SHOW', 
noShowReason: reason } (not rescheduledReason).

Test:
PATCH /api/meetings/:id/status { status: 'NO_SHOW' } with no 
reason → must return 400.
PATCH with { status: 'NO_SHOW', noShowReason: "Client unwell" }
→ 200. DB: SELECT "noShowReason" FROM meetings WHERE id=... 
→ "Client unwell" (not null).

---

BUG-004 — Dashboard totalLeads KPI ignores date-range filter
File: server/src/routes/dashboard.ts

In the dashboard handler, find the totalLeads count query.
Currently: prisma.lead.count() or similar with no date filter.
Fix: apply the same date-range where clause already used for 
other metrics:

const dateWhere = from && to ? {
  createdAt: { gte: new Date(from), lte: new Date(to) }
} : {}

prisma.lead.count({ 
  where: { ...roleWhere, ...dateWhere } 
})

Apply the same dateWhere to: leadsToday, leadsThisWeek, 
leadsThisMonth, conversionRates, sourceBreakdown, stageFunnel.
Do NOT apply dateWhere to pipelineValue (that should always 
reflect current active pipeline, not historical).
Do NOT apply to outstanding, collectedThisMonth (those have 
their own date logic already).

Test: GET /api/dashboard?from=2026-01-01&to=2026-03-31
→ totalLeads: 0 (no leads created in that range in test DB).
GET /api/dashboard (no params, defaults to this month)
→ totalLeads: correct count for June 2026.

---

BUG-005 — Lead list Status filter is client-side only
File: server/src/routes/leads.ts, client/src/pages/LeadList.tsx

Server fix:
Add ?status= param handling in GET /api/leads:
  const statusToStages: Record<string, string[]> = {
    ACTIVE: ['EFFECTIVE_LEAD','MQL','DQL','PROPOSAL_READY',
             'PROPOSAL_PRESENTED','ONBOARDING'],
    ON_HOLD: ['ON_HOLD'],
    INACTIVE: ['INACTIVE']
  }
  if (status && statusToStages[status]) {
    where.stage = { in: statusToStages[status] }
  }

Note: if both ?stage= and ?status= are provided, ?stage= takes 
precedence (more specific). Add this guard.

Client fix — LeadList.tsx:
Remove the client-side visibleLeads filter for status.
Instead, include status in the server query params when fetching:
  if (filters.status) params.append('status', filters.status)

Test: 
GET /api/leads?status=ON_HOLD → only ON_HOLD stage leads returned.
GET /api/leads?status=ACTIVE → all non-hold non-inactive leads.
GET /api/leads?status=INACTIVE → only INACTIVE leads.
GET /api/leads?status=ACTIVE&stage=MQL → stage= wins, returns MQL only.

---

BUG-006 — Role-restricted endpoints return 401 instead of 403
File: server/src/middleware/auth.ts

Find the requireRole([]) helper. Currently returns 401 when role 
doesn't match. Change to:
  if (!allowedRoles.includes(req.user.role)) {
    return res.status(403).json({ 
      error: "Forbidden — insufficient role",
      required: allowedRoles,
      current: req.user.role
    })
  }

Reserve 401 only for: missing Authorization header, expired token, 
invalid token (the verifyToken middleware, not requireRole).

Test:
Login as BL (valid token). 
GET /api/admin/sla-config with BL token → must return 403 
(not 401, not redirect to login).
Confirm verifyToken still returns 401 for missing/bad token:
GET /api/admin/sla-config with no token → 401.
GET /api/admin/sla-config with tampered token → 401.

---

BUG-009 — SLA breach list includes HANDED_OVER leads
Files: server/src/jobs/slaCheck.ts, server/src/routes/sla.ts, 
server/src/routes/dashboard.ts

Two parts:

Part A — prevent new breaches on closed leads (slaCheck.ts):
Already excludes INACTIVE/ON_HOLD/HANDED_OVER from stage-specific 
rules. But FIRST_CONTACT_24H may not exclude HANDED_OVER.
Confirm all 4 rules exclude: { notIn: ['INACTIVE', 'ON_HOLD', 
'HANDED_OVER'] }. Add HANDED_OVER if missing.

Part B — auto-resolve existing breaches on closure:
In leads.ts PATCH /:id/stage handler — when stage moves to 
HANDED_OVER or INACTIVE, resolve all open SLA breaches for that lead:

await prisma.sLABreach.updateMany({
  where: { leadId: lead.id, resolvedAt: null },
  data: { resolvedAt: new Date() }
})

Also set lead.isSLABreached = false in the same update.

Part C — dashboard breach panel:
In dashboard.ts, the SLA breaches query — add filter:
where: { resolvedAt: null, lead: { stage: { notIn: 
  ['INACTIVE', 'ON_HOLD', 'HANDED_OVER'] } } }

Test: 
Confirm X0013, X0012, X0011 (all HANDED_OVER) now have 
resolvedAt set in DB after running migrate + server restart 
(via the stage-change backfill, or run a one-time fix query).
GET /api/sla/breaches → none of those leads appear.
GET /api/dashboard → needsAttention count drops accordingly.

---

BUG-008 — All offers inactive, active-offer UI path untested
File: seed script or direct DB fix

This is a data issue, not a code bug. Fix:
In server startup or a seed script, if no active offers exist, 
set one offer to isActive=true. Or simply:
PATCH /api/offers/<any existing offer id>/toggle { isActive: true }
via the admin panel during QA setup.

No code change needed. Verify the toggle actually works:
PATCH /api/offers/:id/toggle { isActive: true }
→ GET /api/offers → that offer shows isActive: true
→ /settings page → that offer renders as colored (active) pill.

---

BUG-010 — ?status= server param silently ignored
Already fixed by BUG-005 server-side fix above (same file, same 
location). Confirm it's handled in the same PR — do not duplicate.

---

BUG-011 — No-show reason not enforced server-side
Already fixed by BUG-003 server-side fix above. Confirm the 400 
validation fires on the API even without a UI call.

---

AFTER ALL FIXES:

1. Run npx prisma migrate dev --name bug_fixes_module15
2. Run npx prisma generate
3. Restart server → GET /api/health → 200

Verify each fix with these specific tests:

V1. GET /api/leads/export?stage=MQL → download CSV, confirm 
    only MQL leads, correct columns, Content-Type: text/csv.

V2. Open /whatsapp → open Templates picker → confirm 6 templates 
    listed with labels and body text. Select one → textarea fills.

V3. PATCH /api/meetings/:id/status { status: "NO_SHOW" } with 
    no reason → 400. With noShowReason → 200. DB: noShowReason 
    persisted (not null).

V4. GET /api/dashboard?from=2026-01-01&to=2026-03-31 → 
    totalLeads: 0. GET /api/dashboard (no params) → 
    totalLeads: correct June count.

V5. GET /api/leads?status=ON_HOLD → only ON_HOLD leads.
    GET /api/leads?status=INACTIVE → only INACTIVE leads.

V6. BL token + GET /api/admin/sla-config → 403 (not 401).
    No token + GET /api/admin/sla-config → 401.

V7. Move a HANDED_OVER lead (X0013) via 
    PATCH /api/leads/:id/stage { stage: "HANDED_OVER" } 
    (it's already there — just confirm) then check DB: 
    SLABreach.resolvedAt for that leadId is not null.
    GET /api/dashboard → those leads absent from breach panel.

V8. PATCH /api/offers/:id/toggle { isActive: true } → 200.
    Open /settings → that offer shows as colored active pill.

SUMMARY REPORT FORMAT:
For each V1-V8:
  ✅ PASS — evidence (response or DB row)
  ❌ FAIL — exact error + file:line

List every file modified.
Confirm: grep -rn "eyJ" client/src/ → empty.
Confirm: npx prisma migrate status → no pending migrations.